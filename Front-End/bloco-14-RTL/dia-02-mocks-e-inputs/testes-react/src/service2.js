const toUpperCase = (word) => word.toUpperCase();

const firstLetter = (word) => word.split('')[0];

const concatStrings = (word1, word2) => word1 + word2;


module.exports = { toUpperCase, firstLetter, concatStrings };